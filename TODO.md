# TODO

## Improvements
- [ ] Refactor script into functions
- [ ] Add README.md

---

## DONE
- [x] Fix available and used disk size being doubled
- [x] Standardize disk section commands using `df -h /` instead of `df -h --total`
- [x] `lsb_release` compatibility with other distros is not guaranteed
- [x] Add logged users and login failures
- [x] Reduce repetition of `free` and `df` calls using a different variable reading approach
- [x] Remove redundancy in remaining sections

